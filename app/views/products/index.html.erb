<%= link_to "new", new_product_path %>
<table>
<tr>
<th>Product Name</th>
<th>Description</th>
<th></th>
<th></th>
</tr>
<% @products.each do |product| %>
<tr>
<td><%= product.name%></td>
<td><%= product.desc%></td>
<td><%= link_to "edit", edit_product_path(product) %> | <%= link_to "delete", product_path(product),:method => 'DELETE' %> |  <%= link_to "show", product_path(product) %></td>
<% end %>
</table>
